{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake build",
            "command": "make -j$(nproc)",
            "options": {
                "cwd": "${workspaceFolder}/build2"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ],
    "version": "2.0.0"
}
